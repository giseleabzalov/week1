function addTwoNums(a,b) {
    return a + b
}

const add2Nums = (a, b) => {
    return a + b
}

const squareNum = a => {
    return a ** 2
}

// if the functionis only one line
// we can leave off {} and the RETURN

const squareNum2 = a => a ** 2  //arrow is the FUNCTION

console.log(squareNum2(5))

const aspectRatio = (w, h) => w / h

const sayHey = () => console.log("Ho")